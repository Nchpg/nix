# This is a template file for personal home.nix settings.
{
  config = {
    userSettings = {
      git = {
        userName = "xavier.login"; 
        userEmail = "xavier.login@example.com";
      };
    };
  };
}